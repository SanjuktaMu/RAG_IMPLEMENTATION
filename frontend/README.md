Frontend React (Vite)

Run:
1) cd frontend
2) npm install
3) npm run dev

Optional API base URL:
- Create .env with:
  VITE_API_BASE=http://localhost:8000

Features:
- Upload PDF to FastAPI /api/upload
- Ask questions via FastAPI /api/query
- Show answer and retrieved context
